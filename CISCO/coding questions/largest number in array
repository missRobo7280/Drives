#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;cin>>n;
    int a[n];
    int maxx=INT_MIN;
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
        if(a[i]>maxx)
        maxx=a[i];
    }
    cout<<maxx;
    
}

//second largest in array
